services:
  - type: web
    name: genai-knowledge-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.api.rag_api:app --host 0.0.0.0 --port 10000